import { OnboardingStageChip } from '.'
import React from 'react'

describe('OnboardingStageChip', () => {
  it('should match snapshot', () => {
    // const wrapper = mount(<OnboardingStageChip />)
    // expect(wrapper).toMatchSnapshot()
  })
})
