import React from 'react'
import { DescriptionBox } from '.'

describe('DescriptionBox', () => {
  it('should match snapshot', () => {
    // const wrapper = mount(<DescriptionBox />)
    // expect(wrapper).toMatchSnapshot()
  })
})
